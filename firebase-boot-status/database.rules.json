{
  "rules": {
    "boot_ack": {
      ".read": true,
      "$device": {
        ".write": true,
        ".validate": "newData.hasChildren(['device_id']) && newData.child('device_id').val() == $device"
      }
    }
  }
}
